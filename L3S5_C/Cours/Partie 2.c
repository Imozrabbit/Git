/*1. Operator
    - % : rest of the integer left

    - ++ and -- : incrementation
        - a = ++i : incremented before calculating a
        - a = i++ : incremented after calculating a

    - Booleen logique (travailler sur vrai ou faux)
        - && : and logique
        - || : or logique
        - ! : not logique

    - Logique (travailler sur les bits)
        - & : and
        - | : or
        - ^ : xor
        - ~ : not
        - << n : décalage à gauche de n bits (insertion de 0 à droite) = fois 2^n
        - >> n : décalage à droite de n bits (insertion de 0 à gauche) = divisé par 2^n

    - Forme syntaxique comme += -= *= ...

    - if condition using ? : (condition) ? expression_if_yes : expression_if_false
        - max = (a>b)? a : b
*/


/*2. Branches
    - if (condition) {}
      else if (condition) {}
      else {}

    - switch(variable){
        case valeur1 :
            ...;
            break;
        case valeur2 :
            ...;
            break;
        case ...:
        default :
      }
*/


/*3. Loops
    - evaluate after : do {
                        ...
                    } while (condition)

    - evaluate before : 
        - while (condition) {
              ...
          }
        - for (i=0;i<100;i++) {
              ...
          }
*/